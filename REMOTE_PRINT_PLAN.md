# Remote print workflow

Jarvis should search MakerWorld for a P1S-compatible print profile, present the candidate, wait for explicit user confirmation, then download the sliced 3MF and send it to the printer. Manual 3MF import remains only as a fallback/debug path.
